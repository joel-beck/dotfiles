#!/usr/bin/env zsh

# Enable force click on trackpad
defaults write com.apple.trackpad.forceClick -bool true

# Set trackpad speed scaling (1 is slow, 3 is fast)
defaults write com.apple.trackpad.scaling -int 1

# Enable scrolling on trackpad
defaults write com.apple.trackpad.scrolling -bool true
