# Use Perlbrew to manage Perl versions
# Install perl versions with e.g. `perlbrew install perl-5.32.0`
# Switch to a perl version with e.g. `perlbrew switch perl-5.32.0`
# Then install perl modules with `cpanm Module::Name`, e.g. `cpanm Mac::SystemDirectory`
source ~/perl5/perlbrew/etc/bashrc
